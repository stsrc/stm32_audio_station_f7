create hardcoded sample tiles (B.wav, C.wav, etc)
create metronome button, which turns on and off metronome sound
create 'rec' button
change optimization flags to O3 or Os

make makefile create all object and *.d files in separate build directory
